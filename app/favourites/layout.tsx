import { ReactNode } from "react";

export default function FavouritesLayout({
                                        children,
                                      }: {
  children: ReactNode;
}) {
  return <>{children}</>;
}
